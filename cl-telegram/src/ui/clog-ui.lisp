;;; clog-ui.lisp --- CLOG-based GUI (optional)

(in-package #:cl-telegram/ui)

(defun make-clog-ui ()
  "Create a CLOG-based GUI."
  (error "Not implemented"))

(defun clog-ui-run ()
  "Run the CLOG GUI."
  (error "Not implemented"))

(defun clog-ui-show-chat (chat-id)
  "Show a chat in the GUI."
  (declare (ignore chat-id))
  (error "Not implemented"))

(defun clog-ui-update-message (message)
  "Update message display in GUI."
  (declare (ignore message))
  (error "Not implemented"))
